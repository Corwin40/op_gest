<form method="post" action="{{ path('op_admin_accompagnement_delete', {'id': accompagnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accompagnement.id) }}">
    <button class="btn">Delete</button>
</form>
