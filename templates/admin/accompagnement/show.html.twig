{% extends 'admin.html.twig' %}

{% block title %}Accompagnement{% endblock %}

{% block content %}
    <h1>Accompagnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accompagnement.id }}</td>
            </tr>
            <tr>
                <th>Accompagnement</th>
                <td>{{ accompagnement.accompagnement ? accompagnement.accompagnement|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ accompagnement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_admin_accompagnement_index') }}">back to list</a>

    <a href="{{ path('op_admin_accompagnement_edit', {'id': accompagnement.id}) }}">edit</a>

    {{ include('admin/accompagnement/_delete_form.html.twig') }}
{% endblock %}
