{% extends 'admin.html.twig' %}

{% block title %}question{% endblock %}

{% block content %}
    <h1>question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ question.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ question.slug }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ question.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_question_index') }}">back to list</a>

    <a href="{{ path('admin_question_edit', {'id': question.id}) }}">edit</a>

    {{ include('admin_question/_delete_form.html.twig') }}
{% endblock %}
