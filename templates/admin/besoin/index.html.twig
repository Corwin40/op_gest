{% extends 'admin.html.twig' %}

{% block title %}Besoin index{% endblock %}

{% block content %}
    <h1>Besoin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for besoin in besoins %}
            <tr>
                <td>{{ besoin.id }}</td>
                <td>{{ besoin.name }}</td>
                <td>
                    <a href="{{ path('admin_besoin_show', {'id': besoin.id}) }}">show</a>
                    <a href="{{ path('admin_besoin_edit', {'id': besoin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_besoin_new') }}">Create new</a>
{% endblock %}
