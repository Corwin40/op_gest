{% extends 'base.html.twig' %}

{% block title %}Besoin{% endblock %}

{% block body %}
    <h1>Besoin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ besoin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ besoin.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_besoin_index') }}">back to list</a>

    <a href="{{ path('admin_besoin_edit', {'id': besoin.id}) }}">edit</a>

    {{ include('admin_besoin/_delete_form.html.twig') }}
{% endblock %}
